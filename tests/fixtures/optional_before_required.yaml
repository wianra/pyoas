openapi: 3.0.0
info:
  title: Reproduction
  version: 1.0.0
paths:
  /repro:
    post:
      operationId: repro_op
      parameters:
        - name: optional_param
          in: query
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Req'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Res'
components:
  schemas:
    Req:
      type: object
      properties:
        foo:
          type: string
    Res:
      type: object
      properties:
        bar:
          type: string
