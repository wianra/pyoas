openapi: "3.0.3"
info:
  title: Circular Ref Test
  version: "1.0.0"
tags:
  - name: trees
paths:
  /trees:
    get:
      operationId: listTrees
      tags: [trees]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TreeNode"
components:
  schemas:
    TreeNode:
      type: object
      properties:
        value:
          type: string
        children:
          type: array
          items:
            $ref: "#/components/schemas/TreeNode"
